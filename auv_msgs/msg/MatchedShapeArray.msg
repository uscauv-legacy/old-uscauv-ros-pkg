Header header
# All of the shapes are assumed to come from the same image and thus should share the same header data (time and reference frame)

auv_msgs/MatchedShape[] shapes

uint64[] hierarchy

uint32 image_rows
uint32 image_cols